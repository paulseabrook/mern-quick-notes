import * as notesAPI from './notes-api';

export function getNotes() {
  notesAPI
    .indexDisc()
    .then((res) => res.json())
    .then((res) => onNotesSuccess(res.notes));
}

export function onNotesSuccess(notes) {
  if (notes) {
    return notes;
  } else {
    return null;
  }
}
